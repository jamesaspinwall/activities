<%= form_for(task) do |f| %>
  <% if task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :context %>
    <%= f.text_field :context %>
  </div>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :tags %>
    <%= f.text_field :tags %>
  </div>

  <div class="field">
    <%= f.label :data %>
    <%= f.text_area :data %>
  </div>

  <div class="field">
    <%= f.label :complete_ts %>
    <%= f.number_field :complete_ts %>
  </div>

  <div class="field">
    <%= f.label :active_ts %>
    <%= f.number_field :active_ts %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
